import { CATALOG_API_PATHS } from "@/constants/catalog-api";

/** Django DRF v1 pricelist endpoints (relative to HMIS_API_URL, server-only). */
export const PRICELISTS_API_PATHS = CATALOG_API_PATHS.pricelists;
